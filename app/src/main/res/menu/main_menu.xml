<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/searchItem"
        android:icon="@android:drawable/ic_menu_search"
        android:title="@string/search"
        app:showAsAction="always"
        app:actionViewClass="android.support.v7.widget.SearchView" />

    <item
        android:id="@+id/moreItem"
        android:title="@string/more"
        android:icon="@drawable/more_action"
        app:showAsAction="ifRoom">

        <menu>
            <item
                android:id="@+id/cardMenuItem"
                android:title="@string/card_view"/>
            <item
                android:id="@+id/fullViewMenuItem"
                android:title="@string/full_view"/>
            <item
                android:id="@+id/gridViewMenuItem"
                android:title="@string/grid_view"/>
        </menu>

    </item>

</menu>